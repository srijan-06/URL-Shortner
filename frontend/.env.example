# Base URL of the deployed backend API.
# Leave empty in local dev — Vite proxies /api to http://localhost:4000.
# In production (Vercel) set this to your Render/Railway backend URL, e.g.
#   VITE_API_BASE_URL=https://your-shortener.onrender.com
VITE_API_BASE_URL=
